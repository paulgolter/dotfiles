# keychain manages ssh-agent reuse across sessions — no need to
# spawn ssh-agent ourselves.
if status --is-interactive; and type -q keychain
    eval (keychain --eval --agents ssh github.com gitlab.com)
end
